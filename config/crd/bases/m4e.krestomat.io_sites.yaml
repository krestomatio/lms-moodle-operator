---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.8.0
  creationTimestamp: null
  name: sites.m4e.krestomat.io
spec:
  group: m4e.krestomat.io
  names:
    kind: Site
    listKind: SiteList
    plural: sites
    singular: site
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Age of the resource
      jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - description: Site status such as Unknown/SettingUp/Ready/Failed/Terminating
        etc
      jsonPath: .status.state
      name: STATUS
      type: string
    - description: Time of latest transition
      jsonPath: .status.conditions[?(@.type=='Ready')].lastTransitionTime
      name: SINCE
      type: date
    - description: Flavor name
      jsonPath: .spec.flavor
      name: FLAVOR
      type: string
    - description: Site URL
      jsonPath: .spec.m4eSpec.moodleHost
      name: HOST
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Site is the Schema for the sites API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: SiteSpec defines the desired state of Site
            properties:
              flavor:
                description: Flavor defines what M4e flavor to use
                maxLength: 255
                minLength: 1
                type: string
              keydbSpec:
                description: KeydbSpec defines Keydb spec to deploy optionally
                properties:
                  keydbExtraConfig:
                    description: KeydbExtraConfig contains extra keydb config
                    type: string
                  keydbImage:
                    description: KeydbImage defines image for keydb container
                    maxLength: 255
                    type: string
                  keydbMode:
                    description: KeydbMode describes mode keydb runs
                    enum:
                    - standalone
                    - multimaster
                    - custom
                    type: string
                  keydbPvcDataSize:
                    description: KeydbPvcDataSize defines keydb storage size
                    maxLength: 20
                    minLength: 2
                    type: string
                  keydbPvcDataStorageAccessMode:
                    description: KeydbPvcDataStorageAccessMode defines keydb storage
                      access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  keydbPvcDataStorageClassName:
                    description: KeydbPvcDataStorageClassName defines keydb storage
                      class
                    maxLength: 63
                    minLength: 2
                    type: string
                  keydbResourceLimits:
                    description: KeydbResourceLimits whether resource limits are set
                    type: boolean
                  keydbResourceLimitsCpu:
                    description: KeydbResourceLimitsCpu set cpu for resource limits
                    maxLength: 20
                    type: string
                  keydbResourceLimitsMemory:
                    description: KeydbResourceLimitsMemory set memory for resource
                      limits
                    maxLength: 20
                    type: string
                  keydbResourceRequests:
                    description: KeydbResourceRequests whether resource requests are
                      set
                    type: boolean
                  keydbResourceRequestsCpu:
                    description: KeydbResourceRequestsCpu set cpu for resource requests
                    maxLength: 20
                    type: string
                  keydbResourceRequestsMemory:
                    description: KeydbResourceRequestsMemory set memory for resource
                      requests
                    maxLength: 20
                    type: string
                  keydbSize:
                    description: KeydbSize defines keydb number of replicas
                    format: int32
                    type: integer
                  keydbTolerations:
                    description: KeydbTolerations defines any tolerations for Keydb
                      pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              m4eSpec:
                description: M4eSpec defines M4e spec to override Flavor
                properties:
                  moodleConfigAdditionalBlock:
                    description: MoodleConfigAdditionalBlock defines moodle extra
                      block in config.php
                    type: string
                  moodleConfigAdditionalCfg:
                    description: MoodleConfigAdditionalCfg defines moodle extra config
                      properties in config.php
                    type: object
                  moodleConfigSessionRedisCompressor:
                    description: MoodleConfigSessionRedisCompressor defines redis
                      session compresor
                    enum:
                    - none
                    - gzip
                    - zstd
                    type: string
                  moodleConfigSessionRedisPrefix:
                    description: 'MoodleConfigSessionRedisPrefix defines prefix for
                      redis session. Default: '''''
                    maxLength: 100
                    type: string
                  moodleConfigSessionRedisSerializerUseIgbinary:
                    description: 'MoodleConfigSessionRedisSerializerUseIgbinary whether
                      igbinary is used for redis session. Default: false'
                    type: boolean
                  moodleCronjobTolerations:
                    description: MoodleCronjobTolerations defines any tolerations
                      for Moodle cronjob pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  moodleHealthcheckSubpath:
                    description: MoodleHealthcheckSubpath defines Moodle subpath for
                      nginx check
                    maxLength: 100
                    minLength: 2
                    type: string
                  moodleHost:
                    description: MoodleHost defines Moodle host for url
                    maxLength: 100
                    minLength: 2
                    type: string
                  moodleImage:
                    description: MoodleImage defines image for moodle container
                    maxLength: 255
                    type: string
                  moodleKeydbMetaName:
                    description: MoodleKeydbMetaName defines Keydb CR name to use
                      as redis cache.
                    maxLength: 63
                    type: string
                  moodleNewAdminpassHash:
                    description: MoodleNewAdminPassHash is the bcrypt compatible admin
                      password to set in new instance. Required
                    maxLength: 60
                    minLength: 60
                    pattern: ^\$2[ayb]\$.{56}$
                    type: string
                  moodleNewInstance:
                    description: MoodleNewInstance whether new instance job runs
                    type: boolean
                  moodleNewInstanceAdminmail:
                    description: MoodleNewInstanceAdminMail is the admin email to
                      set in new instance. Required
                    maxLength: 100
                    minLength: 3
                    type: string
                  moodleNewInstanceAdminuser:
                    maxLength: 100
                    minLength: 1
                    type: string
                  moodleNewInstanceAgreeLicense:
                    description: MoodleNewInstanceAgreeLicense whether agree to Moodle
                      license. Required
                    type: boolean
                  moodleNewInstanceFullname:
                    maxLength: 100
                    type: string
                  moodleNewInstanceShortname:
                    maxLength: 100
                    type: string
                  moodleNewInstanceSummary:
                    maxLength: 300
                    type: string
                  moodleNfsMetaName:
                    description: MoodleNfsMetaName defines (NFS) Server CR name to
                      use as shared storage for moodledata.
                    maxLength: 63
                    type: string
                  moodlePort:
                    description: MoodlePort defines Moodle port for url
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  moodlePostgresMetaName:
                    description: MoodlePostgresMetaName defines Postgres CR name to
                      use as database.
                    maxLength: 63
                    type: string
                  moodleProtocol:
                    description: MoodleProtocol whether to use http or https
                    enum:
                    - http
                    - https
                    type: string
                  moodlePvcDataSize:
                    description: MoodlePvcDataSize defines moodledata storage size
                    maxLength: 100
                    minLength: 2
                    type: string
                  moodlePvcDataStorageAccessMode:
                    description: MoodlePvcDataStorageAccessMode defines moodledata
                      storage access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  moodlePvcDataStorageClassName:
                    description: MoodlePvcDataStorageClassName defines moodledata
                      storage class
                    maxLength: 63
                    minLength: 2
                    type: string
                  moodleRedisHost:
                    description: 'MoodleRedisHost defines redis host. Default: ''127.0.0.1'''
                    maxLength: 100
                    type: string
                  moodleRedisMucStore:
                    description: 'MoodleRedisMucStore whether redis is configured
                      as MUC store. Default: false'
                    type: boolean
                  moodleRedisMucStoreCompressor:
                    description: 'MoodleRedisMucStoreCompressor defines compressor
                      for redis MUC store. Default: 0'
                    maximum: 1
                    minimum: 0
                    type: integer
                  moodleRedisMucStorePrefix:
                    description: 'MoodleRedisMucStorePrefix defines prefix for redis
                      MUC store. Default: '''''
                    maxLength: 100
                    type: string
                  moodleRedisMucStoreSerializer:
                    description: 'MoodleRedisMucStoreSerializer defines serializer
                      for redis MUC store. Default: 1'
                    maximum: 2
                    minimum: 1
                    type: integer
                  moodleRedisSecret:
                    description: 'MoodleRedisSecret defines redis auth secret name.
                      Default: '''''
                    maxLength: 255
                    type: string
                  moodleRedisSecretAuthKey:
                    description: 'MoodleRedisSecretAuthKey defines key inside auth
                      secret name. Default: ''keydb_password'''
                    maxLength: 100
                    type: string
                  moodleRedisSessionStore:
                    description: 'MoodleRedisSessionStore whether redis is configured
                      as session store. Default: false'
                    type: boolean
                  moodleSubpath:
                    description: MoodleSubpath defines Moodle subpath for url
                    maxLength: 100
                    minLength: 2
                    type: string
                  moodleTolerations:
                    description: MoodleTolerations defines any tolerations for Moodle
                      pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  moodleUpdateMajor:
                    description: 'MoodleUpdateMajor whether major updates are automatically
                      applied. Default: false'
                    type: boolean
                  moodleUpdateMinor:
                    description: 'MoodleUpdateMinor whether minor updates are automatically
                      applied. Default: true'
                    type: boolean
                  nginxExtraConfig:
                    description: NginxExtraConfig contains extra Nginx server config
                    type: string
                  nginxImage:
                    description: NginxImage defines image for nginx container
                    maxLength: 255
                    type: string
                  nginxIngressAnnotations:
                    description: NginxIngressAnnotations defines nginx annotations
                    type: string
                  nginxSize:
                    description: NginxSize defines nginx number of replicas between
                      0 and 255
                    format: int32
                    maximum: 255
                    minimum: 0
                    type: integer
                  nginxTolerations:
                    description: NginxTolerations defines any tolerations for Nginx
                      pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  notifyStatus:
                    description: NotifyStatus specification using ansible URI module
                    properties:
                      method:
                        description: Method The HTTP method of the request or response.
                        enum:
                        - GET
                        - POST
                        - PUT
                        - PATCH
                        - DELETE
                        type: string
                      statusCode:
                        description: StatusCode A list of valid, numeric, HTTP status
                          codes that signifies success of the request.
                        items:
                          type: integer
                        type: array
                      url:
                        description: HTTP or HTTPS URL in the form (http|https)://host.domain[:port]/path
                        type: string
                      uuid:
                        description: UUID used when notifying status to an endpoint
                        maxLength: 36
                        minLength: 36
                        type: string
                    required:
                    - url
                    type: object
                  phpFpmExtraConfig:
                    description: PhpFpmExtraConfig contains extra php-fpm config
                    type: string
                  phpFpmImage:
                    description: PhpFpmImage defines image for Phpfpm container
                    maxLength: 255
                    type: string
                  phpFpmIngressAnnotations:
                    description: PhpFpmIngressAnnotations defines Phpfpm annotations
                    type: string
                  phpFpmPhpExtraIni:
                    description: PhpFpmPhpExtraIni contains extra php ini config
                    type: string
                  phpFpmSize:
                    description: PhpFpmSize defines Phpfpm number of replicas between
                      0 and 255
                    format: int32
                    maximum: 255
                    minimum: 0
                    type: integer
                  phpFpmTolerations:
                    description: PhpFpmTolerations defines any tolerations for Phpfpm
                      pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                required:
                - moodleNewAdminpassHash
                - moodleNewInstanceAdminmail
                - moodleNewInstanceAgreeLicense
                type: object
              nfsSpec:
                description: NfsSpec defines NFS Server spec to override Flavor
                properties:
                  serverConfLogLevel:
                    description: 'ServerConfLogLevel defines nfs log level. Default:
                      EVENT'
                    enum:
                    - "NULL"
                    - FATAL
                    - MAJ
                    - CRIT
                    - WARN
                    - EVENT
                    - INFO
                    - DEBUG
                    - MID_DEBUG
                    - M_DBG
                    - FULL_DEBUG
                    - F_DBG
                    type: string
                  serverExportGroupid:
                    description: ServerExportGroupid defines export folder groupid
                    format: int32
                    type: integer
                  serverExportMode:
                    description: ServerExportMode defines folder permissions mode
                    pattern: '[0-7]{4}'
                    type: string
                  serverExportUserid:
                    description: ServerExportUserid defines export folder userid
                    format: int32
                    type: integer
                  serverGaneshaExtraBlockConfig:
                    description: ServerGaneshaExtraBlockConfig contains extra block
                      in ganesha server config
                    type: string
                  serverImage:
                    description: ServerImage defines image for server container
                    maxLength: 255
                    type: string
                  serverPvcAutoexpansion:
                    description: ServerPvcAutoexpansion enables autoexpansion
                    type: boolean
                  serverPvcAutoexpansionCapGib:
                    description: ServerPvcAutoexpansionCapGib defines limit for autoexpansion
                      increments
                    format: int32
                    type: integer
                  serverPvcAutoexpansionIncrementGib:
                    description: ServerPvcAutoexpansionIncrementGib defines Gib to
                      increment
                    format: int32
                    type: integer
                  serverPvcDataSize:
                    description: ServerPvcDataSize defines server storage size
                    maxLength: 20
                    minLength: 2
                    type: string
                  serverPvcDataStorageAccessMode:
                    description: ServerPvcDataStorageAccessMode defines server storage
                      access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  serverPvcDataStorageClassName:
                    description: ServerPvcDataStorageClassName defines server storage
                      class
                    maxLength: 63
                    minLength: 2
                    type: string
                  serverResourceLimits:
                    description: ServerResourceLimits whether resource limits are
                      set
                    type: boolean
                  serverResourceLimitsCpu:
                    description: ServerResourceLimitsCpu set cpu for resource limits
                    maxLength: 20
                    type: string
                  serverResourceLimitsMemory:
                    description: ServerResourceLimitsMemory set memory for resource
                      limits
                    maxLength: 20
                    type: string
                  serverResourceRequests:
                    description: ServerResourceRequests whether resource requests
                      are set
                    type: boolean
                  serverResourceRequestsCpu:
                    description: ServerResourceRequestsCpu set cpu for resource requests
                    maxLength: 20
                    type: string
                  serverResourceRequestsMemory:
                    description: ServerResourceRequestsMemory set memory for resource
                      requests
                    maxLength: 20
                    type: string
                  serverTolerations:
                    description: ServerTolerations defines any tolerations for Server
                      pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
            required:
            - flavor
            type: object
          status:
            description: SiteStatus defines the observed state of Site
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the resource state
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource. --- This struct is intended for direct
                    use as an array at the field path .status.conditions.  For example,
                    type FooStatus struct{ // Represents the observations of a foo's
                    current state. // Known .status.conditions.type are: \"Available\",
                    \"Progressing\", and \"Degraded\" // +patchMergeKey=type // +patchStrategy=merge
                    // +listType=map // +listMapKey=type Conditions []metav1.Condition
                    `json:\"conditions,omitempty\" patchStrategy:\"merge\" patchMergeKey:\"type\"
                    protobuf:\"bytes,1,rep,name=conditions\"` \n // other fields }"
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for this field, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        --- Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              state:
                default: Unknown
                description: state describes the site state
                enum:
                - Unknown
                - Creating
                - SettingUp
                - Failed
                - Ready
                - Terminating
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
