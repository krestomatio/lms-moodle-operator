
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.4.1
  creationTimestamp: null
  name: flavors.m4e.krestomat.io
spec:
  group: m4e.krestomat.io
  names:
    kind: Flavor
    listKind: FlavorList
    plural: flavors
    singular: flavor
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - description: Age of the resource
      jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    - description: Flavor status such as Unknown/Used/NotUsed/Terminating etc
      jsonPath: .status.state
      name: STATUS
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Flavor is the Schema for the flavors API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: FlavorSpec defines the desired state of Flavor
            properties:
              keydbSpec:
                description: KeydbSpec defines Keydb spec to deploy optionally
                properties:
                  keydbExtraConfig:
                    description: KeydbExtraConfig contains extra keydb config
                    type: string
                  keydbImage:
                    description: KeydbImage defines image for keydb container
                    maxLength: 255
                    type: string
                  keydbMode:
                    description: KeydbMode describes mode keydb runs
                    enum:
                    - standalone
                    - multimaster
                    - custom
                    type: string
                  keydbPvcDataSize:
                    description: KeydbPvcDataSize defines keydb storage size
                    maxLength: 20
                    minLength: 2
                    type: string
                  keydbPvcDataStorageAccessMode:
                    description: KeydbPvcDataStorageAccessMode defines keydb storage
                      access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  keydbPvcDataStorageClassName:
                    description: KeydbPvcDataStorageClassName defines keydb storage
                      class
                    maxLength: 63
                    minLength: 2
                    type: string
                  keydbResourceLimits:
                    description: KeydbResourceLimits whether resource limits are set
                    type: boolean
                  keydbResourceLimitsCpu:
                    description: KeydbResourceLimitsCpu set cpu for resource limits
                    maxLength: 20
                    type: string
                  keydbResourceLimitsMemory:
                    description: KeydbResourceLimitsMemory set memory for resource
                      limits
                    maxLength: 20
                    type: string
                  keydbResourceRequests:
                    description: KeydbResourceRequests whether resource requests are
                      set
                    type: boolean
                  keydbResourceRequestsCpu:
                    description: KeydbResourceRequestsCpu set cpu for resource requests
                    maxLength: 20
                    type: string
                  keydbResourceRequestsMemory:
                    description: KeydbResourceRequestsMemory set memory for resource
                      requests
                    maxLength: 20
                    type: string
                  keydbSize:
                    description: KeydbSize defines keydb number of replicas
                    format: int32
                    type: integer
                  keydbTolerations:
                    description: KeydbTolerations defines any tolerations for Keydb
                      pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              m4eSpec:
                description: M4eSpec defines M4e spec
                properties:
                  moodleConfigAdditionalBlock:
                    description: MoodleConfigAdditionalBlock defines moodle extra
                      block in config.php
                    type: string
                  moodleConfigAdditionalCfg:
                    description: MoodleConfigAdditionalCfg defines moodle extra config
                      properties in config.php
                    type: object
                  moodleConfigSessionRedisCompressor:
                    description: MoodleConfigSessionRedisCompressor defines redis
                      session compresor
                    enum:
                    - none
                    - gzip
                    - zstd
                    type: string
                  moodleConfigSessionRedisPrefix:
                    description: 'MoodleConfigSessionRedisPrefix defines prefix for
                      redis session. Default: '''''
                    maxLength: 100
                    type: string
                  moodleConfigSessionRedisSerializerUseIgbinary:
                    description: 'MoodleConfigSessionRedisSerializerUseIgbinary whether
                      igbinary is used for redis session. Default: false'
                    type: boolean
                  moodleCronjobTolerations:
                    description: MoodleCronjobTolerations defines any tolerations
                      for Moodle cronjob pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  moodleHealthcheckSubpath:
                    description: MoodleHealthcheckSubpath defines Moodle subpath for
                      nginx check
                    maxLength: 100
                    minLength: 2
                    type: string
                  moodleHost:
                    description: MoodleHost defines Moodle host for url
                    maxLength: 100
                    minLength: 2
                    type: string
                  moodleImage:
                    description: MoodleImage defines image for moodle container
                    maxLength: 255
                    type: string
                  moodleKeydbMetaName:
                    description: MoodleKeydbMetaName defines Keydb CR name to use
                      as redis cache.
                    maxLength: 63
                    type: string
                  moodleNewAdminpassHash:
                    description: MoodleNewAdminPassHash is the bcrypt compatible admin
                      password to set in new instance. Required
                    maxLength: 60
                    minLength: 60
                    pattern: ^\$2[ayb]\$.{56}$
                    type: string
                  moodleNewInstance:
                    description: MoodleNewInstance whether new instance job runs
                    type: boolean
                  moodleNewInstanceAdminmail:
                    description: MoodleNewInstanceAdminMail is the admin email to
                      set in new instance. Required
                    maxLength: 100
                    minLength: 3
                    type: string
                  moodleNewInstanceAdminuser:
                    maxLength: 100
                    minLength: 1
                    type: string
                  moodleNewInstanceAgreeLicense:
                    description: MoodleNewInstanceAgreeLicense whether agree to Moodle
                      license. Required
                    type: boolean
                  moodleNewInstanceFullname:
                    maxLength: 100
                    type: string
                  moodleNewInstanceShortname:
                    maxLength: 100
                    type: string
                  moodleNewInstanceSummary:
                    maxLength: 300
                    type: string
                  moodleNfsMetaName:
                    description: MoodleNfsMetaName defines (NFS) Server CR name to
                      use as shared storage for moodledata.
                    maxLength: 63
                    type: string
                  moodlePort:
                    description: MoodlePort defines Moodle port for url
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                  moodlePostgresMetaName:
                    description: MoodlePostgresMetaName defines Postgres CR name to
                      use as database.
                    maxLength: 63
                    type: string
                  moodleProtocol:
                    description: MoodleProtocol whether to use http or https
                    enum:
                    - http
                    - https
                    type: string
                  moodlePvcDataSize:
                    description: MoodlePvcDataSize defines moodledata storage size
                    maxLength: 100
                    minLength: 2
                    type: string
                  moodlePvcDataStorageAccessMode:
                    description: MoodlePvcDataStorageAccessMode defines moodledata
                      storage access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  moodlePvcDataStorageClassName:
                    description: MoodlePvcDataStorageClassName defines moodledata
                      storage class
                    maxLength: 63
                    minLength: 2
                    type: string
                  moodleRedisHost:
                    description: 'MoodleRedisHost defines redis host. Default: ''127.0.0.1'''
                    maxLength: 100
                    type: string
                  moodleRedisMucStore:
                    description: 'MoodleRedisMucStore whether redis is configured
                      as MUC store. Default: false'
                    type: boolean
                  moodleRedisMucStoreCompressor:
                    description: 'MoodleRedisMucStoreCompressor defines compressor
                      for redis MUC store. Default: 0'
                    maximum: 1
                    minimum: 0
                    type: integer
                  moodleRedisMucStorePrefix:
                    description: 'MoodleRedisMucStorePrefix defines prefix for redis
                      MUC store. Default: '''''
                    maxLength: 100
                    type: string
                  moodleRedisMucStoreSerializer:
                    description: 'MoodleRedisMucStoreSerializer defines serializer
                      for redis MUC store. Default: 1'
                    maximum: 2
                    minimum: 1
                    type: integer
                  moodleRedisSecret:
                    description: 'MoodleRedisSecret defines redis auth secret name.
                      Default: '''''
                    maxLength: 255
                    type: string
                  moodleRedisSecretAuthKey:
                    description: 'MoodleRedisSecretAuthKey defines key inside auth
                      secret name. Default: ''keydb_password'''
                    maxLength: 100
                    type: string
                  moodleRedisSessionStore:
                    description: 'MoodleRedisSessionStore whether redis is configured
                      as session store. Default: false'
                    type: boolean
                  moodleSubpath:
                    description: MoodleSubpath defines Moodle subpath for url
                    maxLength: 100
                    minLength: 2
                    type: string
                  moodleTolerations:
                    description: MoodleTolerations defines any tolerations for Moodle
                      pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  moodleUpdateMajor:
                    description: 'MoodleUpdateMajor whether major updates are automatically
                      applied. Default: false'
                    type: boolean
                  moodleUpdateMinor:
                    description: 'MoodleUpdateMinor whether minor updates are automatically
                      applied. Default: true'
                    type: boolean
                  nginxExtraConfig:
                    description: NginxExtraConfig contains extra Nginx server config
                    type: string
                  nginxImage:
                    description: NginxImage defines image for nginx container
                    maxLength: 255
                    type: string
                  nginxIngressAnnotations:
                    description: NginxIngressAnnotations defines nginx annotations
                    type: string
                  nginxSize:
                    description: NginxSize defines nginx number of replicas between
                      0 and 255
                    format: int32
                    maximum: 255
                    minimum: 0
                    type: integer
                  nginxTolerations:
                    description: NginxTolerations defines any tolerations for Nginx
                      pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  notifyStatus:
                    description: NotifyStatus specification using ansible URI module
                    properties:
                      method:
                        description: Method The HTTP method of the request or response.
                        enum:
                        - GET
                        - POST
                        - PUT
                        - PATCH
                        - DELETE
                        type: string
                      statusCode:
                        description: StatusCode A list of valid, numeric, HTTP status
                          codes that signifies success of the request.
                        items:
                          type: integer
                        type: array
                      url:
                        description: HTTP or HTTPS URL in the form (http|https)://host.domain[:port]/path
                        type: string
                      uuid:
                        description: UUID used when notifying status to an endpoint
                        maxLength: 36
                        minLength: 36
                        type: string
                    required:
                    - url
                    type: object
                  phpFpmExtraConfig:
                    description: PhpFpmExtraConfig contains extra php-fpm config
                    type: string
                  phpFpmImage:
                    description: PhpFpmImage defines image for Phpfpm container
                    maxLength: 255
                    type: string
                  phpFpmIngressAnnotations:
                    description: PhpFpmIngressAnnotations defines Phpfpm annotations
                    type: string
                  phpFpmPhpExtraIni:
                    description: PhpFpmPhpExtraIni contains extra php ini config
                    type: string
                  phpFpmSize:
                    description: PhpFpmSize defines Phpfpm number of replicas between
                      0 and 255
                    format: int32
                    maximum: 255
                    minimum: 0
                    type: integer
                  phpFpmTolerations:
                    description: PhpFpmTolerations defines any tolerations for Phpfpm
                      pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                required:
                - moodleNewAdminpassHash
                - moodleNewInstanceAdminmail
                - moodleNewInstanceAgreeLicense
                type: object
              nfsSpec:
                description: NfsSpec defines (NFS) Server spec to deploy optionally
                properties:
                  serverConfLogLevel:
                    description: 'ServerConfLogLevel defines nfs log level. Default:
                      EVENT'
                    enum:
                    - "NULL"
                    - FATAL
                    - MAJ
                    - CRIT
                    - WARN
                    - EVENT
                    - INFO
                    - DEBUG
                    - MID_DEBUG
                    - M_DBG
                    - FULL_DEBUG
                    - F_DBG
                    type: string
                  serverExportGroupid:
                    description: ServerExportGroupid defines export folder groupid
                    format: int32
                    type: integer
                  serverExportMode:
                    description: ServerExportMode defines folder permissions mode
                    pattern: '[0-7]{4}'
                    type: string
                  serverExportUserid:
                    description: ServerExportUserid defines export folder userid
                    format: int32
                    type: integer
                  serverGaneshaExtraBlockConfig:
                    description: ServerGaneshaExtraBlockConfig contains extra block
                      in ganesha server config
                    type: string
                  serverImage:
                    description: ServerImage defines image for server container
                    maxLength: 255
                    type: string
                  serverPvcAutoexpansion:
                    description: ServerPvcAutoexpansion enables autoexpansion
                    type: boolean
                  serverPvcAutoexpansionCapGib:
                    description: ServerPvcAutoexpansionCapGib defines limit for autoexpansion
                      increments
                    format: int32
                    type: integer
                  serverPvcAutoexpansionIncrementGib:
                    description: ServerPvcAutoexpansionIncrementGib defines Gib to
                      increment
                    format: int32
                    type: integer
                  serverPvcDataSize:
                    description: ServerPvcDataSize defines server storage size
                    maxLength: 20
                    minLength: 2
                    type: string
                  serverPvcDataStorageAccessMode:
                    description: ServerPvcDataStorageAccessMode defines server storage
                      access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  serverPvcDataStorageClassName:
                    description: ServerPvcDataStorageClassName defines server storage
                      class
                    maxLength: 63
                    minLength: 2
                    type: string
                  serverResourceLimits:
                    description: ServerResourceLimits whether resource limits are
                      set
                    type: boolean
                  serverResourceLimitsCpu:
                    description: ServerResourceLimitsCpu set cpu for resource limits
                    maxLength: 20
                    type: string
                  serverResourceLimitsMemory:
                    description: ServerResourceLimitsMemory set memory for resource
                      limits
                    maxLength: 20
                    type: string
                  serverResourceRequests:
                    description: ServerResourceRequests whether resource requests
                      are set
                    type: boolean
                  serverResourceRequestsCpu:
                    description: ServerResourceRequestsCpu set cpu for resource requests
                    maxLength: 20
                    type: string
                  serverResourceRequestsMemory:
                    description: ServerResourceRequestsMemory set memory for resource
                      requests
                    maxLength: 20
                    type: string
                  serverTolerations:
                    description: ServerTolerations defines any tolerations for Server
                      pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
              postgresSpec:
                description: PostgresSpec defines Postgres spec to deploy optionally
                properties:
                  pgbouncerExtraConfig:
                    description: PgbouncerExtraConfig contains extra pgbouncer config
                    type: string
                  pgbouncerReadonlyExtraConfig:
                    description: PgbouncerReadonlyExtraConfig contains extra pgbouncerReadonly
                      config
                    type: string
                  pgbouncerReadonlyResourceLimits:
                    description: PgbouncerReadonlyResourceLimits whether resource
                      limits are set
                    type: boolean
                  pgbouncerReadonlyResourceLimitsCpu:
                    description: PgbouncerReadonlyResourceLimitsCpu set cpu for resource
                      limits
                    maxLength: 20
                    type: string
                  pgbouncerReadonlyResourceLimitsMemory:
                    description: PgbouncerReadonlyResourceLimitsMemory set memory
                      for resource limits
                    maxLength: 20
                    type: string
                  pgbouncerReadonlyResourceRequests:
                    description: PgbouncerReadonlyResourceRequests whether resource
                      requests are set
                    type: boolean
                  pgbouncerReadonlyResourceRequestsCpu:
                    description: PgbouncerReadonlyResourceRequestsCpu set cpu for
                      resource requests
                    maxLength: 20
                    type: string
                  pgbouncerReadonlyResourceRequestsMemory:
                    description: PgbouncerReadonlyResourceRequestsMemory set memory
                      for resource requests
                    maxLength: 20
                    type: string
                  pgbouncerReadonlyTolerations:
                    description: PgbouncerReadonlyTolerations defines any tolerations
                      for PgbouncerReadonly pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  pgbouncerResourceLimits:
                    description: PgbouncerResourceLimits whether resource limits are
                      set
                    type: boolean
                  pgbouncerResourceLimitsCpu:
                    description: PgbouncerResourceLimitsCpu set cpu for resource limits
                    maxLength: 20
                    type: string
                  pgbouncerResourceLimitsMemory:
                    description: PgbouncerResourceLimitsMemory set memory for resource
                      limits
                    maxLength: 20
                    type: string
                  pgbouncerResourceRequests:
                    description: PgbouncerResourceRequests whether resource requests
                      are set
                    type: boolean
                  pgbouncerResourceRequestsCpu:
                    description: PgbouncerResourceRequestsCpu set cpu for resource
                      requests
                    maxLength: 20
                    type: string
                  pgbouncerResourceRequestsMemory:
                    description: PgbouncerResourceRequestsMemory set memory for resource
                      requests
                    maxLength: 20
                    type: string
                  pgbouncerTolerations:
                    description: PgbouncerTolerations defines any tolerations for
                      Pgbouncer pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  postgresExtraConfig:
                    description: PostgresExtraConfig contains extra postgres config
                    type: string
                  postgresImage:
                    description: PostgresImage defines image for postgres container
                    maxLength: 255
                    type: string
                  postgresMode:
                    description: PostgresMode describes mode postgres runs
                    enum:
                    - standalone
                    - readreplicas
                    type: string
                  postgresPvcDataSize:
                    description: PostgresPvcDataSize defines postgres storage size
                    maxLength: 20
                    minLength: 2
                    type: string
                  postgresPvcDataStorageAccessMode:
                    description: PostgresPvcDataStorageAccessMode defines postgres
                      storage access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  postgresPvcDataStorageClassName:
                    description: PostgresPvcDataStorageClassName defines postgres
                      storage class
                    maxLength: 63
                    minLength: 2
                    type: string
                  postgresReadreplicasPvcDataSize:
                    description: PostgresReadreplicasPvcDataSize defines postgres
                      readreplicas storage size
                    maxLength: 20
                    minLength: 2
                    type: string
                  postgresReadreplicasPvcDataStorageAccessMode:
                    description: PostgresReadreplicasPvcDataStorageAccessMode defines
                      postgres readreplicas storage access modes
                    enum:
                    - ReadWriteOnce
                    - ReadOnlyMany
                    - ReadWriteMany
                    type: string
                  postgresReadreplicasPvcDataStorageClassName:
                    description: PostgresReadreplicasPvcDataStorageClassName defines
                      postgres readreplicas storage class
                    maxLength: 63
                    minLength: 2
                    type: string
                  postgresReadreplicasResourceLimits:
                    description: PostgresReadreplicasResourceLimits whether resource
                      limits are set
                    type: boolean
                  postgresReadreplicasResourceLimitsCpu:
                    description: PostgresReadreplicasResourceLimitsCpu set cpu for
                      resource limits
                    maxLength: 20
                    type: string
                  postgresReadreplicasResourceLimitsMemory:
                    description: PostgresReadreplicasResourceLimitsMemory set memory
                      for resource limits
                    maxLength: 20
                    type: string
                  postgresReadreplicasResourceRequests:
                    description: PostgresReadreplicasResourceRequests whether resource
                      requests are set
                    type: boolean
                  postgresReadreplicasResourceRequestsCpu:
                    description: PostgresReadreplicasResourceRequestsCpu set cpu for
                      resource requests
                    maxLength: 20
                    type: string
                  postgresReadreplicasResourceRequestsMemory:
                    description: PostgresReadreplicasResourceRequestsMemory set memory
                      for resource requests
                    maxLength: 20
                    type: string
                  postgresReadreplicasSize:
                    description: PostgresReadreplicasSize defines postgres readreplicas
                      number of replicas
                    format: int32
                    type: integer
                  postgresReadreplicasTolerations:
                    description: PostgresReadreplicasTolerations defines any tolerations
                      for PostgresReadreplicas pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                  postgresResourceLimits:
                    description: PostgresResourceLimits whether resource limits are
                      set
                    type: boolean
                  postgresResourceLimitsCpu:
                    description: PostgresResourceLimitsCpu set cpu for resource limits
                    maxLength: 20
                    type: string
                  postgresResourceLimitsMemory:
                    description: PostgresResourceLimitsMemory set memory for resource
                      limits
                    maxLength: 20
                    type: string
                  postgresResourceRequests:
                    description: PostgresResourceRequests whether resource requests
                      are set
                    type: boolean
                  postgresResourceRequestsCpu:
                    description: PostgresResourceRequestsCpu set cpu for resource
                      requests
                    maxLength: 20
                    type: string
                  postgresResourceRequestsMemory:
                    description: PostgresResourceRequestsMemory set memory for resource
                      requests
                    maxLength: 20
                    type: string
                  postgresSize:
                    description: PostgresSize defines postgres number of replicas
                    format: int32
                    type: integer
                  postgresTolerations:
                    description: PostgresTolerations defines any tolerations for Postgres
                      pods.
                    items:
                      description: The pod this Toleration is attached to tolerates
                        any taint that matches the triple <key,value,effect> using
                        the matching operator <operator>.
                      properties:
                        effect:
                          description: Effect indicates the taint effect to match.
                            Empty means match all taint effects. When specified, allowed
                            values are NoSchedule, PreferNoSchedule and NoExecute.
                          type: string
                        key:
                          description: Key is the taint key that the toleration applies
                            to. Empty means match all taint keys. If the key is empty,
                            operator must be Exists; this combination means to match
                            all values and all keys.
                          type: string
                        operator:
                          description: Operator represents a key's relationship to
                            the value. Valid operators are Exists and Equal. Defaults
                            to Equal. Exists is equivalent to wildcard for value,
                            so that a pod can tolerate all taints of a particular
                            category.
                          type: string
                        tolerationSeconds:
                          description: TolerationSeconds represents the period of
                            time the toleration (which must be of effect NoExecute,
                            otherwise this field is ignored) tolerates the taint.
                            By default, it is not set, which means tolerate the taint
                            forever (do not evict). Zero and negative values will
                            be treated as 0 (evict immediately) by the system.
                          format: int64
                          type: integer
                        value:
                          description: Value is the taint value the toleration matches
                            to. If the operator is Exists, the value should be empty,
                            otherwise just a regular string.
                          type: string
                      type: object
                    type: array
                type: object
            required:
            - m4eSpec
            type: object
          status:
            description: FlavorStatus defines the observed state of Flavor
            properties:
              state:
                default: Unknown
                description: state describes the site state
                enum:
                - Unknown
                - Creating
                - SettingUp
                - Failed
                - Ready
                - Terminating
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
